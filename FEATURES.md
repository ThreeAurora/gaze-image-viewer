# Gaze 全量功能清单（FEATURES）

[English](./FEATURES.en-US.md) | 中文

本文档是 Gaze 现有功能的**穷尽式清单**，依据：全量开发提交、todo.md 唯一账本、SETTINGS_MATRIX.md 的 166 个设置键、源码键名/菜单/扩展名扫描交叉整理。README 只保留精选门面；这里才是全部。逐键设置状态另见 [SETTINGS_MATRIX.md](SETTINGS_MATRIX.md)。

## 一、视图与浏览

- 三栏布局：目录树 / 文件网格 / 预览面板，六类面板（树/预览/地址栏/工具栏/状态栏/信息栏）各自独立开关并持久化
- 8 种查看方式：缩略图、缩略图+文件名、+标签、+详细信息、图标、列表、详细信息、瀑布流
- 布局方案：跟随上次窗口状态、保存当前布局（命名+防保留名冲突）、删除布局；每布局存 geometry/splitter/panes 三件套
- 多标签页：查看器形态顶部标签栏；常驻「浏览器」标签；标签左侧 32px 缩略图；Ctrl+W 关闭；关到最后一个图片标签自动回浏览器；同文件多标签/单标签/上限（默认 20）可设
- 后退/前进导航（Alt+←/→，到头置灰）；后退到上级时自动定位选中刚离开的子文件夹
- F5 刷新（同目录重载不丢滚动位）；切换目录滚动条归顶
- 地址栏：单击全选、二次点击落光标、回车跳转目录/文件、粘贴带引号路径兼容、跳转后定位选中+就近贴边滚动、500ms 回车宽限防误开查看器、Alt+退格删字、Ctrl+退格删词、路径历史下拉
- 进入文件夹默认选中第一项并联动预览；选中行自动滚入视野（首尾行可见、选中项贴底）
- 快速滚动渲染优化；任何情况下文件不许半隐藏
- 文件树与当前目录双向自动同步；树滚动定位居中
- 文件树左键按下瞬间切换文件夹（展开箭头列豁免）；按住左键扫过逐个切换；「切换/拖动多选」二选一设置
- 树中显示「桌面」开关、显示隐藏项开关
- 拖放：从资源管理器拖入导航/移动到文件夹，Ctrl+拖放复制，拖放确认弹窗可关；拖出网格/树显示禁止光标
- 窗口标题模板自定义：浏览器/查看器各一套，支持 {文件夹}{文件名} 占位符
- 启动行为：带文件/不带文件/指定目录启动、记录文件名、恢复上次目录/文件、启动时打开文件列表和预览框
- 单实例运行：再次启动经 IPC 唤起已有窗口并传路径；命令行传文件路径直接打开

## 二、缩略图

- 三级解码管线：Qt 原生解码 → Windows Shell 缩略图 → 外部 ffmpeg 兜底；缺失时降级并一次性日志提示
- 文件夹四合一缩略图：XnView MP 形态（文件夹剪影内嵌 2×2 小图），不足四格显示实有数；本级不足按自然序扫直接子目录补格；视频格走进程内 ffmpeg 单帧通道；每格向原图取 max(256, 2×格子) 再降采样
- 文件夹单封面模式（四合一关闭时取本级第一候选）
- 视频缩略图：抽帧位置可配（默认 80%），亚秒视频越界自动回落重试，ffprobe 时长探测结果缓存
- HDR 视频缩略图：PQ/HLG 检流 → zscale+tonemap 二次重取（SDR 零额外开销）
- 缩略图尺寸：8 档预设 + 自定义（48–1024px），Ctrl+=/Ctrl+- 或滚轮缩放
- 缩略图缓存 thumbnails.db（SQLite WAL，每线程独立连接）；缓存代际机制保证改设置后真重生成
- 隐藏文件着色显示；文件夹回落图标运行时生成并按尺寸缓存
- 缩略图数据库维护对话框：统计 / 按目录聚合 / 清空 / 重建

## 三、预览与查看器

- 预览面板八类预览：图片 / GIF / 视频 / 音频 / txt / Markdown / PDF / RAW
- 查看器：Enter/双击进入、ESC 返回；双击分流（图像/视频进查看器，其他类型交系统默认应用，300ms 自管阈值）
- 缩放：Ctrl+滚轮光标中心缩放、长按左键 1:1 像素查看（落点聚焦光标）、拖拽平移、导航小图蓝框可拖动定位
- 自动缩放两套配置（普通/全屏）、切文件重置开关、放大/缩小抗独各一档、像素比 10 项、HiDPI 1px=1px、Gamma 纠正、锐化、两段式渲染、预读一幅/保持当前图像
- 查看器外观：背景色、棋盘格挡板、显示边框、滚动条、平移工具、选中覆盖样式、颜色标记显示
- 全屏三层：F11 界面全屏 / G 全屏预览（只铺画面、退出精确还原布局）/ 查看器内全屏；浮动工具条、隐藏指针、信息角标（尺寸/缩放%）、播放条/工具条/滚动条显隐、双显示器选择——全屏独立配置组
- 文本预览：txt 开关 + 超长自动截断
- Markdown 渲染预览（自写渲染器：标题/粗斜删/行内码/围栏码块/引用/列表/任务列表/链接/图片/分隔线/表格/自动链接）
- PDF 预览：内置 Ghostscript 渲染（vendor/gs 随包分发，AGPL 许可证随附）
- RAW 预览：占位说明 +「加载原始RAW」按钮，点击后台线程 LibRaw 全解，解码中切文件自动作废，成功后接管缩放/拖动/查看器全套
- 预览区文本滚轮=切换文件（不滚文本）
- 信息面板（F9）：文件属性 + 图像尺寸 + EXIF 分组树；RGB 直方图（三通道混合叠加 + 亮度白线，后台取样）
- 视频切换闪帧根治：原生视频窗隐藏 + 首帧到达才显示

## 四、视频播放

- 播放控制栏：播放/暂停、停止、音量按钮+数值、进度条点击/拖动跳转、时间显示自适应、剩余时间切换
- 左键单击画面=播放/暂停；自动播放开关、循环播放开关
- 快进/快退 Ctrl+PgUp/PgDn，步长 1–3600s 可设
- GIF 引擎：QImageReader 逐帧解码；进度条贴底、点击跳转、前后双向擦洗、片尾回卷继续播、48MB 帧缓存围绕播放头淘汰；禁用动画开关（GIF/JIF/APNG/ANI）
- 动态照片（Motion Photo）：Apple Live Photo 外部配对视频 + Google/Samsung JPEG 内嵌 MP4 双协议四级检测；内嵌视频 faststart 修复；单击播放、播完回静态帧；右键「播放实况视频」「拆帧保存」；音频伴侣自动播放开关
- AV1：libdav1d 版 avcodec + 拒绝故障硬解设备（二者缺一不可），满速软解
- HDR 视频：VP9 10bit / BT.2020 / PQ(HDR10) 60fps 连续实时播放

## 五、音频与波形

- 10 种音频扩展（mp3/wav/flac/aac/ogg/wma/m4a/opus/amr/ac3），预览面板播放+控制条
- 音频波形：专属低优先级线程流式解码，512 桶 min/max，渐进绘制，代次闸废帧；失败只显示「波形不可用」，绝不影响切文件/加载（性能红线）
- 多采样格式支持（Int16/Int32/Float/UInt8）多声道取跨声道极值
- 8/8 音频编码实测通过（ac3/amr/flac/m4a/mp3/ogg/opus/wav）

## 六、格式与解码支持（首要要求：全格式）

| 类别 | 清单 |
|---|---|
| 图片 33 扩展名 | jpg jpeg jfif png gif bmp webp heic heif hif avif avifs jxl tiff tif ico svg svgz tga icns wbmp pbm pgm ppm xbm xpm cur exr dds qoi jp2 dpx apng |
| RAW 26 扩展名 | cr2 cr3 crw nef nrw arw srf sr2 dng orf rw2 raf pef erf rwl 3fr fff gpr kdc k25 mef mrw x3f mos srw iiq |
| 视频 28 容器 | mp4 mov avi mkv webm wmv flv m4v mpg mpeg 3gp ts m2ts mts vob ogv divx rm rmvb asf f4v avchd mxf qt 3g2 ogm m1v m2v |
| 音频 10 扩展 | mp3 wav flac aac ogg wma m4a opus amr ac3 |
| 文档 14 | txt doc docx pdf rtf odt xls xlsx ppt pptx csv md epub mobi |
| 可执行 12 | exe bat cmd ps1 sh msi com scr vbs jar py pl rb |

- 分流解码管线：AVIF/JXL → ffmpeg（libdav1d/libjxl）；HEIC/HEIF/HIF → WIC；CMYK JPEG → WIC 色彩管理解码；其余 Qt 原生
- 文件头嗅探识别（avif ftyp brand、JXL 双魔数），按扩展名或扫文件头识别开关
- RAW 独立白名单：绝不进缩略图/预读管线，LibRaw 0.21.4 静态编入（真实 CR2 样本 3881ms 解出 6264×4180）
- 全部解码依赖随包分发：vendor/ffmpeg、vendor/gs、vendor/jpegtran、thirdparty/LibRaw——不默认用户装有任何组件

## 七、文件管理与删除

- 文件右键菜单约 25 项：打开/全屏/打开方式/在资源管理器中显示/打开全部选中/新标签打开/剪切/复制/粘贴/复制到.../移动到.../删除/重命名/创建副本/新建文件夹/打印(Ctrl+P)/旋转翻转(左90右90水平垂直)/无损裁剪/颜色标记子菜单/属性/播放实况视频/拆帧保存
- 文件页空白处右键：新建文件夹/在资源管理器中显示/全选/属性
- 右键未选中条目=先自动选中再弹菜单（资源管理器同款）
- 文件夹树右键 13 项：新建/剪切/复制/粘贴/删除/重命名/复制到/移动到/显示子文件夹文件/搜索/资源管理器打开/属性
- 删除：统一回收站路径（SHFileOperationW ALLOWUDDO+WANTNUKEWARNING）；Del/S 两键；删除确认（文件夹独立开关）；删除后选中下一项；左下角浮动提示（文案/配色/停留时长可设）
- 剪贴板：剪切/复制/粘贴（Ctrl+C/X/V），粘贴重名按「重复文件命名」模板
- 创建副本、复制到/移动到对话框、新建文件夹（X 键）
- 重命名：F2/F3 焦点路由（树/网格通用）；对话框或卡片就地改名开关；名称校验（拒绝路径分隔符/非法字符）
- 最近文件菜单：上限 0–100 可设、退出时清理开关

## 八、排序·筛选·搜索

- 表头 16 列排序：文件名/扩展名/修改日期/创建日期/EXIF 拍摄日期/EXIF 修改日期/类型/大小/图像大小/宽度/高度/方向/比例/打印尺寸/路径/颜色标签，升降序
- EXIF 轻量解析（只读头 64KB 取拍摄时间）；创建日期 ctime 实装
- 名称顺序三式：数字顺序（自然排序）/字母顺序/正常顺序
- 启动默认排序 10 档（含记住上次）
- 混合文件/文件夹排序、文件夹总按字母序、新文件加末尾、自动选中新文件、按字节显示大小、显示子文件夹文件
- 筛选 18 模式：全部/图像(+目录)/视频(+目录)/音频/压缩/文档/可执行/文件夹/自定义扩展名/红橙黄绿蓝标/非红
- 自定义扩展名集合筛选（设置持久化）
- 工具栏格式筛选下拉 + 指示器联动
- 内联搜索 Ctrl+F：输入即搜/上一个/下一个/高亮/空值置灰
- 名称搜索对话框：包含/排除双正则、递归、含隐藏、含文件夹；分时预算扫描界面不冻、结果边扫边出、深度/命中数上限
- 以文搜图（万象图搜客户端，Ctrl+Shift+F）：自动拉起后端服务、模型切换（中文/英文 CLIP）、排序、结果网格+来源分数角标、右键定位/打开/复制路径、双击回主窗口定位、服务生命周期管理

## 九、设置项（~166 键 + 3 拼接组，20 分类页）

完整逐键状态见 [SETTINGS_MATRIX.md](SETTINGS_MATRIX.md)。分组概览：

- **General/Start**：单实例、EXIF 方向旋转、DPI 调整、会话保存三档；带文件/不带文件/记录文件名启动
- **Browser**：启动目录/上次目录文件、预览背景色、颜色标记显示、查看方式/名称顺序/筛选模式/自定义扩展名/启动默认排序/路径历史等 20 键
- **FileList**：隐藏项、识别方式（扩展名/文件头三档）、混合排序、新文件位置、字节显示、子文件夹等 10 键
- **FileOps**：删除确认（文件/文件夹分档）、回收站、删除提示与时长、拖放确认、无损备份/保留元数据、重命名方式、重复命名模板
- **Appearance**：缩略图尺寸、阴影/边框/间距、标签排列、标签底色映射（扩展名→颜色可编辑）、深/浅主题
- **Viewer/Preview**：自动缩放、缩放模式、抗锯齿、像素比、背景/挡板/边框/滚动条、自动播放/循环、动画禁用、Gamma、锐化、快进秒数、循环文件列表、两段式渲染、预读；txt/MD/PDF 预览开关
- **Keyboard/Mouse**：左右键/上下键/空格/ESC 行为；修饰键组合的左中右键与滚轮 12 键映射
- **Interface**：多标签/单标签/上限、关视图同步浏览器、启动面板显隐、最近文件上限与清理、标题模板×2、幻灯间隔
- **LabelColors**：扩展名→文件名底色映射与默认色
- **Print**：打印对话框 15 键全持久化（打印机/纸张/份数/横向/每页张数/适应/边距/间距/图注/字号/灰度/背景/边框/页码子集/范围）
- **ImgSearch**：以文搜图端口/目录/python/退出随杀
- **Integration**：资源管理器右键集成（含目录背景）、便携或 %APPDATA% ini 位置迁移
- **Layout/Fullscreen**：布局方案组、全屏独立配置组
- **快捷键**：浏览器全部带键菜单动作可配（Shortcuts/*）+ 查看器 8 动作可配

## 十、界面与主题

- 深/浅双主题：30+ 色彩令牌 + 全应用 QSS，「视图→主题」切换（重启生效）
- 分层灰阶：菜单栏>工具条>树>列表>预览；Win 风格带箭头滚动条四态
- 全局仿 XnView MP 打磨：排序表头、右键菜单、播放控制栏、纯黑背景统一
- 工具栏五处菜单按钮 ▼ 指示；列数下拉；菜单/工具栏勾选状态全面持久化
- 设置页 5 分页 20 分类：搜索过滤、动画、紧凑化、死项清零
- 悬停高亮、卡片选中、蓝框逐像素对齐

## 十一、快捷键（全表）

C/←/↑ 上一个 · V/→/↓ 下一个 · 空格 播放暂停 · Ctrl+PgUp/PgDn 快退/快进 · 按住右键+滚轮 缩放 · 双击预览区 全屏 · F5 刷新 · F11 界面全屏 · G 全屏预览 · ESC 返回 · Alt+←/→ 后退前进 · Backspace 上级 · Alt+退格 地址栏删字 · Ctrl+1~5 颜色标记 · Ctrl+0/D 取消标记 · F 红标 · F2/F3 重命名 · Del/S 删除 · X 新建文件夹 · Enter/双击 查看器 · Ctrl+A 全选 · Ctrl+I 反选 · Ctrl+F 内联搜索 · Ctrl+W 关标签 · Ctrl+P 打印 · Ctrl+O 打开 · Ctrl+C/X/V 剪贴板 · Ctrl+Shift+F 以文搜图 · F12 设置 · F9 信息面板 · 全局对话框空格=确认 · 文本框内裸键不抢占

## 十二、打印·编辑·元数据

- 打印：打印机选择（无打印机引导）、纸张、份数、横向、每页张数、适应方式、边距/间距 mm、图注+字号、灰度、背景、边框、页码子集/范围；15 项全持久化；多图排版布局引擎；右键/文件菜单双入口
- 无损旋转/翻转：jpegtran -copy all 路线+恢复时间戳；仅改 EXIF 方向开关；无损/备份/保留元数据开关；非 JPEG 走 ffmpeg 无损变换
- 无损裁剪：缩放预览拖框选区 → MCU 16 对齐 → jpegtran -perfect 校验
- EXIF 元数据：两层读取器，信息面板分组展示；EXIF 方向自动旋转
- 动态照片拆帧保存（ffmpeg）

## 十三、稳定性与诊断

- Gaze.log 全级别分类落盘；启动检查点链（各阶段毫秒）；GUI 心跳+看门狗（>12s 告警）；崩溃 minidump
- perf.log 性能日志（关键路径耗时）
- 单实例 IPC、双击独立打开快路径、QSettings 线程安全、外部工具黑窗统一静默
- 删除统一回收站路径（铁律）；DB 维护参数化转义

## 十四、其他

- 便携式：Gaze.ini + thumbnails.db 随程序目录，不写注册表；拷目录即迁移
- 系统集成：资源管理器目录/背景右键「用 Gaze 浏览」、打开方式注册
- 构建：CMake + Qt 6.8.3 MinGW，POST_BUILD 自动同步 assets/vendor

## 附：口径与边界（如实说明）

- 图片白名单与解码探针中均无 **PCX/PSD**——对外宣传口径以此文档为准（早期 README 表格中的 PCX/PSD 为过度声明，已订正）
- EXR/DDS/JP2 解码器在位但样张验证未完成
- HEIF/CMYK 当前经系统 WIC 通道解码，是「依赖全自带」的已知例外（见 Roadmap）
- 浅色主题已实现（切换需重启）；运行时热切换仍在 Roadmap
- 明确否决（做了=犯错）：批量重命名、幻灯片、批量转换、类型角标、★标记/评级体系、换基座、两级渐进加载、默认依赖系统组件
